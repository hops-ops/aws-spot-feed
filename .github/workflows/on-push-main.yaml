name: on-push-main
on:
  push:
    branches:
    - main
    paths:
      - '.github/workflows/on-push-main.yaml'
      - '.github/workflows/on-version-tagged.yaml'
      - '.gitops/**'
      - 'apis/**'
      - 'examples/**'
      - 'functions/**'
      - 'tests/**'
      - 'upbound.yaml'

permissions:
  id-token: write
  packages: write
  contents: write
  issues: write
  pull-requests: write

jobs:

  validate:
    uses: unbounded-tech/workflows-crossplane/.github/workflows/validate.yaml@v3.0.0
    with:
      examples: |
        [
          { "example": "examples/spotfeeds/standard.yaml" },
          { "example": "examples/spotfeeds/custom-bucket.yaml" }
        ]
      api_path: apis/spotfeeds
      error_on_missing_schemas: true

  test:
    uses: unbounded-tech/workflows-crossplane/.github/workflows/test.yaml@v3.0.0

  e2e:
    uses: unbounded-tech/workflows-crossplane/.github/workflows/e2e.yaml@v3.0.0
    with:
      aws: true
      aws-use-oidc: true
      aws-account-id: "034489662075"
      aws-region: us-east-2
      write-env-files: true
      timeout-minutes: 34
      env-vars: |
        {
          "SPOT_FEED_BUCKET_NAME": "${{ vars.SPOT_FEED_BUCKET_NAME }}",
          "SPOT_FEED_SUBSCRIPTION_EXTERNAL_NAME": "${{ vars.SPOT_FEED_SUBSCRIPTION_EXTERNAL_NAME }}"
        }

  version-and-tag:
    name: Version and Tag
    needs:
      - validate
      - test
      - e2e
    uses: unbounded-tech/workflow-vnext-tag/.github/workflows/workflow.yaml@v1.21.0
    secrets:
      DEPLOY_KEY: ${{ secrets.DEPLOY_KEY }}
    with:
      useDeployKey: true
